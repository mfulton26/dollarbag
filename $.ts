/**
 * Bag of symbols for safely augmenting JavaScript built-in objects.
 *
 * @module
 */

/** Extensible interface for the bag of symbols. */
// deno-lint-ignore no-empty-interface
export interface DollarSign {}
/** The bag of symbols for safely augmenting JavaScript built-in objects. */
export default {} as DollarSign;
